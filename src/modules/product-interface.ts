export type TProduct = {
  name: string;
  price: number;
  stock: number;
  quantity: number;
  description: string;
  images: string;
  category: string;
};
export type TCategory = {
  name: string;
  category: string;
};
