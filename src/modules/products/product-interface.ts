export type TProduct = {
  name: string;
  price: number;
  category: string;
  description: string;
  images: string[];
  stock: number;
  quantity: number;
};
export type TCategory = {
  name: string;
  category: string;
};
